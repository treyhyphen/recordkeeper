# Copy to .env and fill in real values (keep .env mode 0600; it is gitignored).
DATABASE_URL=postgresql://recordkeeper:***@127.0.0.1:5432/recordkeeper

# Per-account credentials live in accounts.json (mode 0600, gitignored) — copy
# accounts.example.json and fill in one entry per connected account. Last.fm
# entries need `api_key` (and `api_secret` once authenticated writes are enabled).
